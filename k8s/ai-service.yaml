apiVersion: apps/v1
kind: Deployment
metadata:
  name: ai-service
  namespace: lets-connect
  labels:
    app: ai-service
    tier: backend
    version: v1
spec:
  replicas: 2
  selector:
    matchLabels:
      app: ai-service
  template:
    metadata:
      labels:
        app: ai-service
        tier: backend
        version: v1
    spec:
      containers:
      - name: ai-service
        image: lets-connect/ai-service:latest
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 8007
          name: http
          protocol: TCP
        env:
        - name: PORT
          value: "8007"
        - name: NODE_ENV
          value: "production"
        - name: GEMINI_MODEL
          value: "gemini-2.5-flash"
        - name: GEMINI_API_KEY
          valueFrom:
            secretKeyRef:
              name: lets-connect-secrets
              key: GEMINI_API_KEY
        - name: REDIS_URL
          valueFrom:
            configMapKeyRef:
              name: lets-connect-config
              key: REDIS_URL
        resources:
          requests:
            memory: "256Mi"
            cpu: "100m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: /health
            port: 8007
          initialDelaySeconds: 30
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /health
            port: 8007
          initialDelaySeconds: 10
          periodSeconds: 5
          timeoutSeconds: 5
          failureThreshold: 3
---
apiVersion: v1
kind: Service
metadata:
  name: ai-service
  namespace: lets-connect
  labels:
    app: ai-service
spec:
  selector:
    app: ai-service
  ports:
  - port: 8007
    targetPort: 8007
    protocol: TCP
    name: http
  type: ClusterIP
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: ai-service-hpa
  namespace: lets-connect
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: ai-service
  minReplicas: 2
  maxReplicas: 10
  metrics:
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: 70
  - type: Resource
    resource:
      name: memory
      target:
        type: Utilization
        averageUtilization: 80
  behavior:
    scaleDown:
      stabilizationWindowSeconds: 300
      policies:
      - type: Percent
        value: 50
        periodSeconds: 15
    scaleUp:
      stabilizationWindowSeconds: 0
      policies:
      - type: Percent
        value: 100
        periodSeconds: 15
      - type: Pods
        value: 2
        periodSeconds: 15
      selectPolicy: Max
